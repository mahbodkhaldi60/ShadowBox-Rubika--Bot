

const { MAIN_KEYPAD } = require("../keybord/AllKeybord");
const { sendSmartMessage } = require("../services/rubikaApi.service");
const { randomData } = require("../../RandomMessage")


async function startHandler(user, chatId) {
}


module.exports = {
    startHandler
}